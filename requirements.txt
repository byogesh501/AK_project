# AK Visual Intelligence Platform
# ================================
#
# A multi-domain visual inspection and anomaly detection system.
# PCB/electronics inspection is the first supported domain.
#
# PROJECT POLICY: No pretrained models or pretrained weights.
# Every model architecture is defined and trained from scratch.
#
# Quick start:
#   1. python -m venv .venv
#   2. .venv\Scripts\activate        (Windows)
#      source .venv/bin/activate     (Linux/macOS)
#   3. pip install -r requirements.txt
#   4. pytest                        (run tests)
#
# See PROJECT_STATUS.md for the roadmap.

# ── Minimal dependencies ──────────────────────────────────
# Core
torch>=2.0
torchvision>=0.15    # Used for transforms/utilities only — NOT for pretrained models
numpy>=1.24
Pillow>=9.0

# Configuration
PyYAML>=6.0

# API
fastapi>=0.100
uvicorn>=0.22
httpx>=0.24          # Required by FastAPI TestClient

# Dashboard
streamlit>=1.24

# Testing
pytest>=7.0

# Utilities
tqdm>=4.65
matplotlib>=3.7
